export default function BoxResearch( {student, link, color, border_shape, bold_color} ) {
    return (
        <div>
            <div>
                {student["name"]}
            </div>
        </div>
    )
}